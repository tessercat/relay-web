{
  "name": "relay-client",
  "version": "0.0.1",
  "description": "Verto relay client",
  "main": "client.js",
  "scripts": {
    "prebuild": "rm -f ../static/js/client-*.js",
    "build": "esbuild src/client.js --bundle --format=esm --entry-names=[dir]/[name]-[hash] --outdir=../static/js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tessercat/relay-web.git"
  },
  "author": "Peter Christensen",
  "license": "CC-BY-NC-SA-4.0",
  "bugs": {
    "url": "https://github.com/tessercat/relay-web/issues"
  },
  "homepage": "https://github.com/tessercat/relay-web#readme",
  "devDependencies": {
    "esbuild": "^0.14.46",
    "eslint": "^8.18.0"
  }
}
